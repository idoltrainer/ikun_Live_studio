<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.idoltrainer.webvideo.mapper.FavourMapper">

    <resultMap id="BaseResultMap" type="com.idoltrainer.webvideo.domain.Favour">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="videoId" column="videoId" jdbcType="BIGINT"/>
            <result property="userId" column="userId" jdbcType="BIGINT"/>
            <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,videoId,userId,
        createTime,updateTime
    </sql>

    <select id="listFavourPostByPage"
            resultType="com.idoltrainer.webvideo.domain.Video">
        select p.*
        from post p
                 join (select videoId from favour where userId = #{userId}) pf
                      on p.id = pf.videoId ${ew.customSqlSegment}
    </select>
</mapper>
